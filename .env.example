# 复制为 .env 后按需修改。

# ---------- 数据库 ----------
# 宿主机跑 migrate/seed 时用（连 localhost:5432）
DATABASE_URL=postgresql://app:app@localhost:5432/pet_booking

# Docker 里 backend 连的库：不填则用 compose 的 postgres 容器；用 RDS 时填（需 ?sslmode=require）
# BACKEND_DATABASE_URL=postgresql://app:app@postgres:5432/pet_booking
# BACKEND_DATABASE_URL=postgresql://用户:密码@RDS端点:5432/库名?sslmode=require

# ---------- 后端 ----------
JWT_SECRET=dev-jwt-secret
JWT_EXPIRES_IN=7d

# ---------- 前端与 CORS（本地可不改）----------
NEXT_PUBLIC_API_URL=http://localhost:3001/api
CORS_ORIGIN=http://localhost:3000
